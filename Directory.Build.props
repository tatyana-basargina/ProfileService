<Project>
  <PropertyGroup Condition="'$(USE_DOCKER_NUGET)' == 'true'">
    <LocalPackages>/packages</LocalPackages>
  </PropertyGroup>

  <PropertyGroup Condition="'$(USE_DOCKER_NUGET)' != 'true'">
    <LocalPackages>../../nuget-packages</LocalPackages>
  </PropertyGroup>
</Project>
